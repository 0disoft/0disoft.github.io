{
  "title": "Mensor",
  "summary": "Deterministic contract checker for server-rendered HTML applications edited by agents. Validates forms, action inputs, source roles, and import boundaries as JSON."
}
