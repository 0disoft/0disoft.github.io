---
// src/components/SEO.astro
//
// @file SEO 컴포넌트
// @description 페이지 제목과 설명을 기반으로 SEO 관련 메타 태그를 생성하는 컴포넌트입니다.
//              HTML의 <head> 섹션에 포함되어 검색 엔진 최적화를 돕습니다.
// @version 1.0.0

/**
 * @interface Props
 * @description SEO 컴포넌트의 props 인터페이스.
 * @property {string} title - 페이지 제목. <title> 태그 및 Open Graph, Twitter 카드 제목에 사용됩니다.
 * @property {string} description - 페이지 설명. <meta name="description"> 태그 및 Open Graph, Twitter 카드 설명에 사용됩니다.
 */
interface Props {
  title: string;
  description: string;
}

// 부모 컴포넌트로부터 전달받은 제목과 설명을 구조 분해 할당합니다.
const { title, description } = Astro.props;
---

<title>{title}</title>

<meta name="description" content={description} />

<meta property="og:title" content={title} />
<meta property="og:description" content={description} />

<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
