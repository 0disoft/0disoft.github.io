---
// --- 안에 있는 코드는 서버에서 단 한 번만 실행됩니다.
// JSONPlaceholder API에서 게시글 1개의 데이터를 가져옵니다.
const response = await fetch("https://jsonplaceholder.typicode.com/posts/1");
const post = await response.json();
---

<div
  style="border: 1px solid #ccc; padding: 16px; border-radius: 8px; margin-top: 24px;"
>
  <h2>외부 API에서 가져온 게시글:</h2>
  <h3>{post.title}</h3>
  <p>{post.body}</p>
</div>
