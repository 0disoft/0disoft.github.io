---
// src/pages/contact.astro
import { CONTACT_LINKS } from "../consts";
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "Contact";
---

<BaseLayout title={pageTitle}>
  <div class="mb-12">
    <h1 class="font-display text-4xl">Contact Me</h1>
    <p class="text-secondary mt-4">
      I'm always open to discussing new projects, creative ideas, or
      opportunities.
    </p>
  </div>

  <div class="max-w-2xl">
    <div class="flex flex-col gap-6">
      {
        CONTACT_LINKS.map((link) => (
          <a
            href={link.url}
            target="_blank"
            rel="noopener noreferrer"
            class="group flex items-center gap-4 p-5 border border-border rounded-lg no-underline transition-all hover:(border-accent bg-elementHoverBg)"
          >
            <div
              class:list={[
                link.icon,
                "w-8 h-8 text-secondary transition-colors group-hover:text-accent",
              ]}
            />
            <span class="text-lg font-medium text-primary transition-colors group-hover:text-accent">
              {link.name}
            </span>
          </a>
        ))
      }
    </div>
  </div>
</BaseLayout>
