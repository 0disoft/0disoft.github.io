# UI components

Small shared controls live here when they carry repeated behavior, not just repeated styles.
Keep this folder limited to controls that own accessibility attributes, keyboard hooks, or state styling used in more than one place.
