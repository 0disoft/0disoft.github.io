{
  // @file tsconfig.json
  // @description Astro 프로젝트의 TypeScript 컴파일러 및 언어 서비스 설정을 정의합니다.
  //              이 파일을 통해 프로젝트의 타입 안전성을 보장하고 개발 경험을 향상시킵니다.
  // @version 1.0.0
  /**
   * @property {string} extends
   * @description Astro의 공식 엄격(strict) 타입스크립트 설정을 상속합니다.
   * 이는 Astro 프로젝트에 필요한 기본 설정을 자동으로 포함하여 초기 설정을 간소화합니다.
   * @see https://docs.astro.build/en/guides/typescript/
   */
  "extends": "astro/tsconfigs/strict",
  /**
   * @property {object} compilerOptions
   * @description TypeScript 컴파일러의 동작 방식을 제어하는 옵션들입니다.
   */
  "compilerOptions": {
    /**
     * @property {string} baseUrl
     * @description 모듈 해석을 위한 기본 디렉터리를 지정합니다.
     * 모든 비상대(non-relative) 모듈 임포트의 기준 경로를 프로젝트 루트 디렉터리(`.` )로 설정합니다.
     */
    "baseUrl": ".",
    /**
     * @property {object} paths
     * @description 모듈 임포트 경로에 대한 별칭(alias)을 설정합니다.
     * `@/*` 별칭을 `src/*` 디렉터리에 매핑하여, 상대 경로 대신 절대 경로로 모듈을 쉽게 임포트할 수 있도록 합니다.
     * @example import MyComponent from "@/components/MyComponent.astro";
     */
    "paths": {
      "@/*": [
        "src/*"
      ]
    }
  },
  /**
   * @property {string[]} include
   * @description 컴파일러가 타입스크립트 파일을 찾을 디렉터리 또는 파일 패턴을 지정합니다.
   * 프로젝트의 모든 파일을 타입스크립트 언어 서비스의 분석 대상으로 만듭니다.
   * `.astro/types.d.ts`는 Astro가 자동 생성하는 타입 정의 파일로, 이 파일도 포함해야 타입 안전성이 보장됩니다.
   */
  "include": [
    ".astro/types.d.ts",
    "**/*"
  ],
  /**
   * @property {string[]} exclude
   * @description 컴파일러가 무시할 디렉터리 또는 파일 패턴을 지정합니다.
   * `dist` 디렉터리는 최종 빌드 결과물이므로 컴파일러의 분석에서 제외합니다.
   */
  "exclude": [
    "dist"
  ]
}